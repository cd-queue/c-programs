// wap to calculate the area of circle and modify the same program and find volume of a cylinder


#include <stdio.h>
int main()
{
    int r = 6;

    int height = 10;

    printf("The area of a cicle with radius is %d is: %f\n", r, 3.14*r*r);
    printf("The volume of cylinder with radius %d and heaight %d is %f", r, height, 3.14*r*r*height );
   

    return 0;
    

}